import React from 'react';

export default () => (
  <div>
    <h1>plog</h1>
    <p>
      This is a personal logbook application that can be used to keep notes.
    </p>
    <p>
      Each entry (called post) is organized into different blocks where each one of them has
      a dialect and the contained text. Dialects (currently Markdown and Latex) can be used to
      format text.
    </p>
  </div>
);
